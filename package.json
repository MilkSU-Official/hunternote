{
  "name": "hunternote",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "build:wasm": "cd wasm-tools && wasm-pack build --target web --out-dir ../docs/.vitepress/wasm && cp ../docs/.vitepress/wasm/wasm_tools_bg.wasm ../docs/public/",
    "build:all": "npm run build:wasm && npm run build",
    "preview": "vitepress preview docs"
  },
  "keywords": [
    "security",
    "ctf",
    "re",
    "writeup",
    "vitepress",
    "starneko",
    "牛奶猫",
    "android security",
    "kernel exploitation"
  ],
  "author": "MilkSU (starneko)",
  "license": "CC-BY-NC-4.0",
  "description": "MilkSU Hunternote - MilkSU 的系统安全研究笔记",
  "devDependencies": {
    "@types/animejs": "^3.1.13",
    "@types/node": "^25.2.2",
    "vitepress": "^1.6.4"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "animejs": "^3.2.2",
    "xterm": "^5.3.0"
  }
}
